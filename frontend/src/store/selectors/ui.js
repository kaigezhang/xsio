export const activeModalNameSelector = state => state.ui.activeModalNameSelector;
export const activeModalPropsSelector = state => state.ui.activeModalPropsSelector;

export const uiSelector = state => state.ui;
